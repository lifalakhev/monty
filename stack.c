#include "monty.h"

/**
 * lv_stack - prints the top.
 * @head: stack head
 * @line_number: line number
 * Return: no return
*/
void lv_stack(stack_t **head, unsigned int line_number)
{
	(void)head;
	(void)line_number;
	bus.lifi = 0;
}
