#include "monty.h"
/**
 * stack - prints the top
 * @head: stack head
 * @line_number: line_number
 * Return: no return
*/
void stack(stack_t **head, unsigned int line_number)
{
	(void)head;
	(void)line_number;
	bus.lifi = 0;
}
